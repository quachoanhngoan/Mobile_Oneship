import 'package:flutter/material.dart';

class StepTypeOfService extends StatelessWidget {
  const StepTypeOfService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Step Type of Service'),
      ),
    );
  }
}
